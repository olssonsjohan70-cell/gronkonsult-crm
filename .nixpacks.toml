[phases.setup]
nixPkgs = ["python311", "gcc", "nodejs_22", "npm"]

[phases.install]
cmds = [
  "cd frontend && npm install && npm run build",
  "cd backend && pip install -r requirements.txt"
]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
